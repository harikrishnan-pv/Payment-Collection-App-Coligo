---
scope: Payment Collection App monorepo (apps/api, apps/mobile, packages/shared)
purpose: build substrate for 1-day hiring-test build; graded artifact
altitude: whole system
updated: 2026-09-15T11:24
---

- (decision) Paradigm: layered API (routes->services->repositories) + thin-client mobile (screens -> services/api-client); shared types+validation in packages/shared keeps contract single-sourced
- (decision) Express routes EXACTLY as test mandates: GET /customers, POST /payments, GET /payments/:account_number; nginx exposes them under /api/ (prefix stripped) so mobile uses http://<ip>/api while graders get literal paths
- (decision) customers.id BIGSERIAL PK, account_number VARCHAR(20) UNIQUE; payments.id UUID (gen_random_uuid) as public payment reference, FK customer_id per test schema; money NUMERIC(12,2)
- (decision) Payments are INSERT-only ledger rows (status SUCCESS); no balance mutation in v1; query optimization via unique(account_number) + index payments(customer_id, payment_date)
- (decision) Deploy: CI builds TS, rsyncs to EC2, pm2 reload; Postgres16 docker localhost-only; nginx :80 -> /api/ proxy + static landing; env vars DATABASE_URL / EXPO_PUBLIC_API_URL; secrets only in GH Actions + server .env
- (decision) Error contract: JSON {error:{code,message}} everywhere; zod request validation -> 400; unknown account -> 404; 5xx never leaks stack
- (event) spine finalized (fast path, deadline-driven; reviewer gate compressed to self-check + version pins deferred to lockfiles)
