# Base URL of the Coligo API. Set for production/demo builds:
#   EXPO_PUBLIC_API_URL=http://<server-host>/api
# Leave unset for local development with Expo Go — the app then assumes the
# API runs on port 3000 of the same machine that serves the dev bundle.
# EXPO_PUBLIC_API_URL=http://localhost:3000
